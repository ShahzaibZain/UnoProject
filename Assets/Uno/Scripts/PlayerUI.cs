/*using UnityEngine;
using UnityEngine.UI;

public class PlayerUI : MonoBehaviour
{
    public Text playerNameText;

    // Method to set the player's UI elements
    public void SetPlayerUI(string playerName)
    {
        playerNameText.text = playerName;  // Set player name (Player1, Player2, etc.)
        //transform.position = position.position;  // Set the position of the player UI
    }
}
*/